var romanNumeralsArray = [];
$(document).ready(function() {
  $('form#input').submit(function(event) {
    event.preventDefault();
    var userInput = $('input#number');

    $('#result').text(answer);
  });
});

var cryptoSquare = function(sentence) {
  var string = sentence.replace(/\s+/g, '');

}
